# YOLOv8-pico – custom ultra-small YOLO variant; ideal for TFLite / ESP32-S3 deployment
model_type: yolov8-pico

# Number of detection classes (single class: ball)
nc: 1

# Pretrained weights – set to null to train from scratch using the custom YAML
weights: null
