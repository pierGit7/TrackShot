# @package _global_
defaults:
  - _self_
  - model: default
  - data: default

framework: "pytorch"



training:
  epochs: 500
  lr: 0.001
  batch_size: 32
  force: false  # set to true (or pass training.force=true) to retrain even if a checkpoint exists

seed: 42
